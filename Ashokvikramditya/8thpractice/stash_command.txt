commands:

1. git stash list,
2. git stash save <f.name>
3. git stash pop <f.name>
4. git stash apply <f.name>
5. git stash drop

-> Above commands are used for stashing purpose, this commands are used to store your working file in stashing mod then you can countinue another .

